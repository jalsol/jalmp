#ifndef TYPES_H
#define TYPES_H

#include <cstdint>

enum class ArtistId : int64_t {
	Invalid = -1,
};

enum class TrackId : int64_t {
	Invalid = -1,
};

enum class PlaylistId : int64_t {
	Invalid = -1,
};

#endif // TYPES_H
