// #include "WidgetCapture.hpp"

// WidgetCapture::WidgetCapture(QWidget *parent) : QWidget{parent} {}

// WidgetCapture::WidgetCapture(const QString &capture, QWidget *parent)
// 	: QWidget{parent}, mCapture{capture} {}

// QWidget *WidgetCapture::get() {
// 	if (!mWidget) {
// 		mWidget = findChild<QWidget *>(mCapture);
// 	}

// 	return mWidget;
// }
